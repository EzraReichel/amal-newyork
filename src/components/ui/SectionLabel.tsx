export default function SectionLabel({ children }: { children: React.ReactNode }) {
  return (
    <span className="font-body text-[10px] uppercase tracking-luxury text-amal-ash/60">
      {children}
    </span>
  );
}
